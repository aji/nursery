#!/usr/bin/env python

from distutils.core import setup

setup(name="AATE",
      version="0.4",
      description="Alex's ANSI Terminal Emulator",
      author="Alex Iadicicco",
      packages=["aate"]
      )
